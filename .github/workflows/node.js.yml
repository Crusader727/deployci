name: CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install dependencies
      run: npm install

    - name: Get branch name
      id: branch-name
      uses: tj-actions/branch-names@v4.9
        
    - name: Build
      run: npm run build
      id: build 

    - name: Deploy
      uses: appleboy/scp-action@master
      id: deploy
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USERNAME }}
        passphrase: ${{ secrets.SERVER_PASSPHRAZE }}
        key: ${{ secrets.SERVER_KEY }}
        port: 22
        source: "build/*"
        target: "/var/www/html/${{ steps.branch-name.outputs.current_branch }}"

    - name: Comment
      if: success()
      uses: marocchino/sticky-pull-request-comment@v2
      with:
        header: release
        message: |
          Release is done [URL](http://${{ secrets.SERVER_HOST }}/${{ steps.branch-name.outputs.current_branch }}/build) 🟢🟢🟢

    - name: Comment
      if: failure()
      uses: marocchino/sticky-pull-request-comment@v2
      with:
        header: release
        message: |
          Something went wrong with release ❌❌❌
